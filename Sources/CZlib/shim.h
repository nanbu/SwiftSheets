#ifndef SWIFTSHEETS_CZLIB_SHIM_H
#define SWIFTSHEETS_CZLIB_SHIM_H

/* zlib as one Swift module. Nothing here is ours — the header belongs to whichever zlib the machine
   already has, and this file exists only because Swift needs a header it can name. */
#include <zlib.h>

#endif
