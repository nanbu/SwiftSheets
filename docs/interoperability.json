{
  "meta": {
    "library_version": "1.1.0",
    "measured": "2026-09-23T13:56:46.778392+00:00",
    "swift": "Apple Swift version 6.4 (swiftlang-6.4.0.34.1 clang-2100.3.34.1)\nTarget: arm64-apple-macosx27.0.0",
    "commit": "2c6720aa520e492210188d30072bdee3e84c0425",
    "scope": "Synthetic 48-feature whole-workbook write/read probe; library reader, not an application rendering measurement"
  },
  "directions": [
    {
      "featureCount": 48,
      "from": "numbers",
      "lost": [],
      "sourceFeatures": 23,
      "suggestion": "",
      "to": "numbers",
      "warnings": []
    },
    {
      "featureCount": 48,
      "from": "numbers",
      "lost": [
        "cell controls",
        "quote functions",
        "several tables per sheet"
      ],
      "sourceFeatures": 23,
      "suggestion": "xlsx",
      "to": "ods",
      "warnings": [
        "STOCK fetches live data and OpenFormula has no such function; the cached value is written, the way Numbers itself exports to Excel",
        "1 other table(s) not written: an ODS sheet holds a single table (write .numbers to keep them)",
        "1 other table(s) not written: an ODS sheet holds a single table (write .numbers to keep them)",
        "1 cell control(s) (checkbox, stepper, slider, rating) dropped: ODF has no cell controls — the value is kept (write .numbers to keep the control)"
      ]
    },
    {
      "featureCount": 48,
      "from": "numbers",
      "lost": [
        "cell controls",
        "quote functions",
        "several tables per sheet"
      ],
      "sourceFeatures": 23,
      "suggestion": "",
      "to": "xlsx",
      "warnings": [
        "1 cell control(s) (checkbox, stepper, slider, rating) dropped: Excel has no cell controls — the value is kept (write .numbers to keep the control)",
        "1 other table(s) not written: a worksheet holds a single grid (write .numbers to keep them)",
        "1 other table(s) not written: a worksheet holds a single grid (write .numbers to keep them)",
        "STOCK fetches live data and Excel has no such function; the cached value is written, the way Numbers itself exports it"
      ]
    },
    {
      "featureCount": 48,
      "from": "ods",
      "lost": [
        "CF colour scale",
        "CF data bar",
        "CF icon set",
        "array formulas",
        "auto-filter",
        "calculation settings",
        "consolidation",
        "custom properties",
        "detective arrows",
        "filter criteria",
        "hidden sheets",
        "label ranges",
        "outline groups",
        "page breaks",
        "pivot tables",
        "print area",
        "sheet defined names",
        "sheet protection",
        "sort state",
        "structured tables",
        "tab colour",
        "workbook defined names",
        "workbook protection"
      ],
      "sourceFeatures": 43,
      "suggestion": "xlsx",
      "to": "numbers",
      "warnings": [
        "1 defined name(s) dropped: Numbers has no defined names",
        "workbook protection is dropped: Numbers locks a document with a password, which this writer does not set",
        "1 custom document propert(ies) dropped: Numbers has no free-form document fields",
        "1 named table(s) dropped: every Numbers table is named, but its own header rows are not this frame",
        "the auto-filter and its sort are dropped: a Numbers table filters and sorts through its own rules, which are not written",
        "sheet protection is dropped: Numbers protects a whole document, not a sheet",
        "1 array formula(s) written as the anchor's formula and the covered cells' values: Numbers' own spill function does not survive its load-time recalculation, so the range is not kept (Appendix B.26)",
        "the page number (&P) is drawn where Numbers draws it, in the centre of the footer",
        "the print area, page breaks are dropped: Numbers prints a canvas, not a page grid",
        "1 sheet-scoped name(s) dropped: Numbers has no defined names",
        "the tab colour is dropped: Numbers tabs have no colour",
        "row / column grouping is dropped: Numbers groups by category, not by outline level",
        "conditional format colorScale is dropped: Numbers has no rule of that kind (it drops the same ones when it imports an Excel file)",
        "conditional format dataBar is dropped: Numbers has no rule of that kind (it drops the same ones when it imports an Excel file)",
        "conditional format iconSet is dropped: Numbers has no rule of that kind (it drops the same ones when it imports an Excel file)",
        "pivot table Summary is written as a Numbers pivot: Numbers recomputes it from the source rows, and reading the file back gives its summary as an ordinary table",
        "Numbers has no hidden sheets; the sheet is visible",
        "a calculation setting is dropped — a search condition is a regular expression here, and is not a regular expression there; only OpenDocument keeps it in the file",
        "1 label range(s) — headings usable in formulas is dropped: only OpenDocument has it",
        "the stored consolidation definition is dropped: only OpenDocument has it",
        "1 cell(s) with tracing arrows is dropped: only OpenDocument has it"
      ]
    },
    {
      "featureCount": 48,
      "from": "ods",
      "lost": [],
      "sourceFeatures": 43,
      "suggestion": "",
      "to": "ods",
      "warnings": [
        "pivot table Summary is written as an ODF data pilot: the layout travels, the numbers are recomputed when the file is opened"
      ]
    },
    {
      "featureCount": 48,
      "from": "ods",
      "lost": [
        "calculation settings",
        "consolidation",
        "detective arrows",
        "label ranges"
      ],
      "sourceFeatures": 43,
      "suggestion": "",
      "to": "xlsx",
      "warnings": [
        "a calculation setting is dropped — a search condition is a regular expression here, and is not a regular expression there; only OpenDocument keeps it in the file",
        "1 label range(s) — headings usable in formulas is dropped: only OpenDocument has it",
        "the stored consolidation definition is dropped: only OpenDocument has it",
        "1 cell(s) with tracing arrows is dropped: only OpenDocument has it"
      ]
    },
    {
      "featureCount": 48,
      "from": "xlsx",
      "lost": [
        "CF colour scale",
        "CF data bar",
        "CF icon set",
        "array formulas",
        "auto-filter",
        "custom properties",
        "filter criteria",
        "hidden sheets",
        "outline groups",
        "page breaks",
        "pivot tables",
        "print area",
        "protected ranges",
        "scenarios",
        "sheet defined names",
        "sheet protection",
        "sort state",
        "structured tables",
        "tab colour",
        "workbook defined names",
        "workbook protection"
      ],
      "sourceFeatures": 41,
      "suggestion": "xlsx",
      "to": "numbers",
      "warnings": [
        "3 part(s) preserved from the xlsx file cannot be carried into Numbers",
        "1 defined name(s) dropped: Numbers has no defined names",
        "workbook protection is dropped: Numbers locks a document with a password, which this writer does not set",
        "1 custom document propert(ies) dropped: Numbers has no free-form document fields",
        "1 named table(s) dropped: every Numbers table is named, but its own header rows are not this frame",
        "the auto-filter and its sort are dropped: a Numbers table filters and sorts through its own rules, which are not written",
        "sheet protection is dropped: Numbers protects a whole document, not a sheet",
        "1 protected range(s) dropped: Numbers has no window left open inside a protected sheet",
        "1 array formula(s) written as the anchor's formula and the covered cells' values: Numbers' own spill function does not survive its load-time recalculation, so the range is not kept (Appendix B.26)",
        "1 scenario(s) dropped: Numbers has no scenarios",
        "the page number (&P) is drawn where Numbers draws it, in the centre of the footer",
        "the print area, page breaks are dropped: Numbers prints a canvas, not a page grid",
        "1 sheet-scoped name(s) dropped: Numbers has no defined names",
        "the tab colour is dropped: Numbers tabs have no colour",
        "row / column grouping is dropped: Numbers groups by category, not by outline level",
        "conditional format colorScale is dropped: Numbers has no rule of that kind (it drops the same ones when it imports an Excel file)",
        "conditional format dataBar is dropped: Numbers has no rule of that kind (it drops the same ones when it imports an Excel file)",
        "conditional format iconSet is dropped: Numbers has no rule of that kind (it drops the same ones when it imports an Excel file)",
        "pivot table Summary is written as a Numbers pivot: Numbers recomputes it from the source rows, and reading the file back gives its summary as an ordinary table",
        "pivot table Summary: its banded look is dropped; a Numbers pivot takes the document's table style",
        "Numbers has no hidden sheets; the sheet is visible"
      ]
    },
    {
      "featureCount": 48,
      "from": "xlsx",
      "lost": [
        "protected ranges",
        "scenarios"
      ],
      "sourceFeatures": 41,
      "suggestion": "xlsx",
      "to": "ods",
      "warnings": [
        "pivot table Summary: its banded look is not carried into ODF, which has no pivot style",
        "pivot table Summary is written as an ODF data pilot: the layout travels, the numbers are recomputed when the file is opened",
        "1 scenario(s) dropped: an ODF scenario is a whole shadow sheet, not a named set of input values, so it cannot be written without changing the sheet list",
        "1 protected range(s) dropped: ODF protects a whole table, with no windows left open in it",
        "named table Sales written as an ODF database range: its banded-row style is not carried",
        "3 part(s) preserved from the xlsx file cannot be carried into ODS"
      ]
    },
    {
      "featureCount": 48,
      "from": "xlsx",
      "lost": [],
      "sourceFeatures": 41,
      "suggestion": "",
      "to": "xlsx",
      "warnings": []
    }
  ]
}
