# SwiftSheets の計画

## 次の一手（2026-09-05 時点）

- [x] 暗号を別製品へ — 中核に暗号のコードを入れず、SheetDecrypt（復号）・SheetEncrypt（暗号化）を繋いだアプリだけが持つ（0.17.0・付録 B.39.9 Rev 4.29）
- [x] ODS に画像を書く — 形式で唯一落としていた図を運ぶ（0.16.0・付録 B.43）
- [ ] 逐次読みのピークが行数で伸びる原因を調べる — 200 列の基準で XLSX 19 → 59 → 452 MB（100 万 → 1,000 万 → 1 億マス）。逐次書きは平ら（XLSX 10・ODS 17・CSV 7 MB）なので読み側の持ち物
- [ ] ODS の逐次読みが列数で伸びる原因 — 200 列だと 100 万マスで 202 MB（10 列の 42 MB の 5 倍）
- [ ] 1 億マスの全載せ操作を 32 GB 以上の機械で 1 度測る（8 GB では見込みピーク 30 GB 超で測らない）
- [ ] 書式や図の多い実務のブックを計測台の素材に足す
- [x] 書き出しも全形式で 1 行ずつ — Excel だけの逐次書きを ODS と Numbers にも
- [x] 0.14.0 を出す
- [x] 複数のシートを同時に読んで、多シートのブックを速くする — 同時数が手綱
- [x] 0.13.0 を出す
- [x] ODS・Numbers・CSV も 1 行ずつ読む — 形式が違っても呼び方は 1 つ
- [x] 100 万マスの Numbers 書類で速さとメモリを測り、読みを 2 倍速くする
- [x] ODS のブック構造の保護と Excel の反復計算の設定を運ぶ
- [x] 0.11.2 を出す — Linux 対応と、壊れたファイルで落ちない修正を先に届ける
- [x] ZIP の読み書きを作り替える — 4 GB 超のファイルと、展開爆弾への備え
- [x] 読む前にシートとセル数を問い合わせられるようにし、上限は読む側が決める
- [x] 形式の判定を、ファイル全体を読まずに答える
- [x] 読み込みの字句解析を自前にして速くする
- [x] 大きなシートを切れ端ごとに読み書きしてメモリを抑える
- [x] パスワード付きの Excel と ODS を読み書きする
- [x] 読みたいシートだけ読む・CSV を 1 行ずつ読み書きする
- [x] 速さの計測台を公開し、数字を覚えずに測る
- [x] 0.12.0 を出す

## 書き方

上の節が計画のすべてです。`- [ ]` がこれから、`- [x]` が済み。上から順に、いちばん上が次の一手。
1 行は利用者の言葉で 60 字以内。理由と経緯はコミットの本文と実装仕様書の付録 B にあります。
